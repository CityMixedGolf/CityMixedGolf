@{
    ViewData["Title"] = "Access Denied";
}

<div class="hero-band">
    <div class="container">
        <h1>Access Denied</h1>
        <p>You don't have permission to view that page.</p>
    </div>
</div>

<div class="container mt-4 text-center" style="max-width:480px">
    <div class="golf-panel p-4">
        <i class="ti ti-lock" style="font-size:3rem;color:var(--golf-muted)"></i>
        <p class="mt-3" style="color:var(--golf-muted)">This area is restricted to admin users only.</p>
        <a asp-area="Public" asp-controller="Home" asp-action="Index" class="btn btn-golf-primary mt-2">Back to homepage</a>
    </div>
</div>
